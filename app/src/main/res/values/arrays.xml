<?xml version="1.0" encoding="utf-8"?>
<!--
In LSPosed source code:

list.replaceAll(s ->
    switch (s) {
        case "android" -> "system";
        case "system" -> "android";
        default -> s;
    }
);

Apparently we need to specify android to hook system (for legacy compatibility)
-->
<resources>
    <string-array name="xposed_scope">
        <item>com.android.systemui</item>
        <item>com.oplus.athena</item>
        <item>com.android.launcher</item>
        <item>android</item>
    </string-array>
</resources>